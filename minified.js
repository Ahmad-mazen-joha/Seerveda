import{initializeApp}from "https://www.gstatic.com/firebasejs/11.6.1/firebase-app.js";import{getAuth,signInWithEmailAndPassword,signOut,onAuthStateChanged}from "https://www.gstatic.com/firebasejs/11.6.1/firebase-auth.js";import{getFirestore,doc,onSnapshot,updateDoc}from "https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js";const firebaseConfig={apiKey:"AIzaSyCqacb7HxQX3Ij4HtYKmeYOwvDm6WfKqnA",authDomain:"seerveda-f2d42.firebaseapp.com",projectId:"seerveda-f2d42",storageBucket:"seerveda-f2d42.firebasestorage.app",messagingSenderId:"403504239464",appId:"1:403504239464:web:6d57480fdd60820fcc3cdd",measurementId:"G-JP26CC7Z45"};const app=initializeApp(firebaseConfig);const auth=getAuth(app);const db=getFirestore(app);const loginSection=document.querySelector(".login-card");const adminPanel=document.getElementById("admin-panel");const loginButton=document.getElementById("login-button");const logoutButton=document.getElementById("logout-button");const emailInput=document.getElementById("email");const passwordInput=document.getElementById("password");const loginError=document.getElementById("login-error");const contentSections=document.getElementById("content-sections");const toast=document.getElementById("toast");const overlayBackground=document.querySelector(".overlay");const textareas={management:document.getElementById("management-json"),products:document.getElementById("products-json"),oldEvents:document.getElementById("oldEvents-json"),newEvents:document.getElementById("newEvents-json"),footer:document.getElementById("Footer-json")};let unsubscribe;let removeOverLay=()=>{loginSection.classList.add("hidden");overlayBackground.classList.add("hidden");document.body.classList.remove("no-scroll")};let webVersion="user";let contType="div";let contType2="img";let contType3="input";function normalizeCloudinaryUrl(url){try{if(!url||typeof url!=="string")return url;if(!url.toLowerCase().includes("cloudinary"))return url;const uploadToken="/upload/";const idx=url.indexOf(uploadToken);if(idx===-1)return url;if(url.includes("f_auto")&&url.includes("q_auto"))return url;const before=url.slice(0,idx+uploadToken.length);const after=url.slice(idx+uploadToken.length);return before+"f_auto,q_auto/"+after}catch(e){return url}}
loginButton.addEventListener("click",()=>{const email=String(emailInput.value||"").trim();const password=String(passwordInput.value||"").trim();loginError.textContent="";if(!email||!password){showToast("Please enter both email and password.","error");return}
signInWithEmailAndPassword(auth,email,password).then(userCredential=>{showToast("login sucessed, welcome sir","sucess");webVersion="admine"}).catch(error=>{showToast("Login failed. Please check your password and email.","error");console.error("Login error:",error)})});logoutButton.addEventListener("click",()=>{signOut(auth).then(()=>{window.location.reload()}).catch(error=>{console.error("Logout error:",error);showToast("Logout failed.","error")})});onAuthStateChanged(auth,user=>{if(user){loginSection.classList.add("hidden");adminPanel.classList.remove("hidden");loadAndDisplayData();removeOverLay();contType="input";contType2="input";try{if(loginButton){loginButton.disabled=!0}}catch(e){console.error("Error disabling login button:",e)}}else{adminPanel.classList.add("hidden");contType="div";contType2="img";if(unsubscribe){unsubscribe()}
try{if(loginButton){loginButton.disabled=!1}}catch(e){console.error("Error enabling login button:",e)}}});let productsData=[];let mangementData=[];let eventData=[];let newEventData=[];function generateId(length=8){const chars="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";let result="";for(let i=0;i<length;i++){result+=chars.charAt(Math.floor(Math.random()*chars.length))}
return result}
function countWords(str){if(!str)return 0;return String(str).trim().split(/\s+/).filter(Boolean).length}
function isValidUrl(value){if(!value)return!1;try{new URL(value);return!0}catch(error){return!1}}
function validateEmail(value){return/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value)}
function showFormError(message){showToast(message,"error")}
function validateRequiredFields(fields){for(const field of fields){const el=field.el;if(!el)continue;const value=String(el.value||"").trim();if(!value){showFormError(field.name+" is required.");if(typeof el.focus==="function")el.focus();return!1}
if(field.validator&&!field.validator(value)){showFormError(field.invalidMessage||`Invalid ${field.name.toLowerCase()}.`);if(typeof el.focus==="function")el.focus();return!1}}
return!0}
function ensureUniqueProductIds(arr){if(!Array.isArray(arr))return arr;const seen=new Set();return arr.map(item=>{if(!item)return item;let id=item.id?String(item.id).replace(/\s+/g,""):"";if(!id||seen.has(id)){let newId;do{newId=generateId(10)}while(seen.has(newId));id=newId}
seen.add(id);return{...item,id}})}
function ensureUniqueManagementIds(arr){if(!Array.isArray(arr))return[];const seen=new Set();return arr.map(item=>{if(!item)return item;let id=item.id?String(item.id).trim():"";if(!id||seen.has(id)){let newId;do{newId=generateId(10)}while(seen.has(newId));id=newId}
seen.add(id);return{...item,id}})}
let data;function loadAndDisplayData(){const docRef=doc(db,"websiteContent","main");unsubscribe=onSnapshot(docRef,doc=>{contentSections.classList.remove("hidden");if(doc.exists()){data=doc.data();productsData=ensureUniqueProductIds(data.products||[]);mangementData=ensureUniqueManagementIds(data.management||[]);eventData=data.oldEvents||[];newEventData=data.newEvents||[];const footerTextarea=document.getElementById("Footer-json");if(footerTextarea){footerTextarea.value=JSON.stringify(data.footer,null,2)}
renderProducts();renderManagement();renderEvents();renderNewEvents();return data}else{console.log("No such document!");showToast("Could not load document.","error")}},error=>{console.error("Error fetching document:",error);showToast("Error loading data.","error")})}
window.addEventListener("DOMContentLoaded",user=>{if(user){signOut(auth)}});console.log(unsubscribe);window.saveData=async sectionKey=>{let dataToSave;if(sectionKey==="products"){dataToSave=productsData}else if(sectionKey==="management"){dataToSave=mangementData}else if(sectionKey==="oldEvents"){dataToSave=eventData}else if(sectionKey==="newEvents"){dataToSave=newEventData}else if(sectionKey==="footer"){const footerTextarea=document.getElementById("Footer-json");try{dataToSave=JSON.parse(footerTextarea.value)}catch(error){console.error("Invalid JSON:",error);showToast(`Error: Invalid JSON format in footer.`,"error");return}}else{showToast(`Unknown section: ${sectionKey}`,"error");return}
const docRef=doc(db,"websiteContent","main");try{await updateDoc(docRef,{[sectionKey]:dataToSave});showToast(`${sectionKey} data saved successfully!`,"success")}catch(error){console.error("Error saving document:",error);showToast(`Error saving ${sectionKey}.`,"error")}};let addProductForm=document.querySelector(".addProductForm");const conditionsButton=document.querySelector(".submitConditionButton");const conditionAddingForm=document.querySelector(".conditionAddingForm");const formContainer=document.querySelector(".forms-container");const conditionV2FormEl=document.querySelector(".conditionV2Form");const submitConditionV2Btn=document.querySelector(".submitConditionV2Button");const addImageBtn=document.querySelector(".addImageButton");function createCondV2ImageRow(url="",alt=""){const row=document.createElement("div");row.className="condV2ImageRow";const urlInput=document.createElement("input");urlInput.type="url";urlInput.className="condV2ImageUrl";urlInput.placeholder="Image URL (Cloudinary)";urlInput.value=url;const altInput=document.createElement("input");altInput.type="text";altInput.className="condV2ImageAlt";altInput.placeholder="Alt text (optional)";altInput.value=alt;const removeBtn=document.createElement("button");removeBtn.type="button";removeBtn.className="removeImageButton";removeBtn.textContent="Remove";row.appendChild(urlInput);row.appendChild(altInput);row.appendChild(removeBtn);return row}
if(addImageBtn&&conditionV2FormEl){addImageBtn.addEventListener("click",()=>{const container=conditionV2FormEl.querySelector(".condV2Images");if(!container)return;const current=container.querySelectorAll(".condV2ImageRow").length;if(current>=10){showFormError("Maximum 10 images allowed.");return}
container.appendChild(createCondV2ImageRow())})}
if(conditionV2FormEl){conditionV2FormEl.addEventListener("click",e=>{if(e.target&&e.target.matches(".removeImageButton")){const row=e.target.closest(".condV2ImageRow");if(row)row.remove();}})}
function resetConditionV2Form(){if(!conditionV2FormEl)return;const name=conditionV2FormEl.querySelector(".condV2Name");const date=conditionV2FormEl.querySelector(".condV2Date");const text=conditionV2FormEl.querySelector(".condV2Text");const images=conditionV2FormEl.querySelector(".condV2Images");const pid=conditionV2FormEl.querySelector(".condV2ProductId");if(name)name.value="";if(date)date.value="";if(text)text.value="";if(images){images.innerHTML="";images.appendChild(createCondV2ImageRow())}
if(pid)pid.value="";const wc=conditionV2FormEl.querySelector(".condV2WordCount");if(wc)wc.textContent="0"}
if(submitConditionV2Btn&&conditionV2FormEl){submitConditionV2Btn.addEventListener("click",()=>{const productIdEl=conditionV2FormEl.querySelector(".condV2ProductId");const productId=productIdEl?productIdEl.value:null;const nameEl=conditionV2FormEl.querySelector(".condV2Name");const dateEl=conditionV2FormEl.querySelector(".condV2Date");const textEl=conditionV2FormEl.querySelector(".condV2Text");const imagesContainer=conditionV2FormEl.querySelector(".condV2Images");if(!productId){showFormError("Product not selected. Please choose a product first.");return}
const product=productsData.find(p=>p.id==productId);if(!product){showFormError("Product not found. Please reopen the condition form.");return}
const name=nameEl?nameEl.value.trim():"";const date=dateEl?dateEl.value:"";const text=textEl?textEl.value.trim():"";const wordCount=countWords(text);if(!name){showFormError("Condition name is required.");return}
if(wordCount>200){showFormError("Text must be 200 words or fewer.");return}
const imageRows=imagesContainer?Array.from(imagesContainer.querySelectorAll(".condV2ImageRow")):[];const images=imageRows.map(r=>{const url=(r.querySelector(".condV2ImageUrl")||{}).value||"";const alt=(r.querySelector(".condV2ImageAlt")||{}).value||"";return{url:url.trim(),alt:alt.trim()}}).filter(i=>i.url);if(images.length<1||images.length>10){showFormError("Please provide between 1 and 10 images.");return}
for(const img of images){if(!isValidUrl(img.url)){showFormError("Please use valid image URLs for all condition images.");return}}
images.forEach(img=>{img.url=normalizeCloudinaryUrl(img.url)});const newItem={id:generateId(10),name,date,text,images};product.conditionsV2=product.conditionsV2||[];product.conditionsV2.push(newItem);resetConditionV2Form();conditionV2FormEl.classList.add("hidden");formContainer.classList.add("hidden");renderProducts()})}
if(conditionV2FormEl){const textEl=conditionV2FormEl.querySelector(".condV2Text");const wcEl=conditionV2FormEl.querySelector(".condV2WordCount");if(textEl&&wcEl){textEl.addEventListener("input",()=>{wcEl.textContent=countWords(textEl.value)})}}
const closeForm=form=>{if(!form)return;form.classList.add("hidden");formContainer.classList.add("hidden");if(typeof form.reset==="function"){form.reset()}
if(form===addProductForm){saveButton.classList.add("hidden");editSaveButton.classList.add("hidden")}};formContainer.addEventListener("click",event=>{if(event.target.matches(".cancelFormButton")){const form=event.target.closest("form");closeForm(form)}});const saveProductData=(saveType,editedProductId)=>{console.log(saveType,editedProductId);const productTitle=String(document.querySelector(".productTitle").value||"").trim();const productType=String(document.querySelector(".productType").value||"").trim();const productImage=String(document.querySelector(".productMainImage").value||"").trim();const productDesc=String(document.querySelector(".productDescription").value||"").trim();const productLongDesc=String(document.querySelector(".productLongDescription").value||"").trim();const productMainImage=String(document.querySelector(".productImage").value||"").trim();const newProductFilter=document.querySelector(".newProduct").checked;if(saveType==="add"){if(!productTitle||!productType||!productImage||!productDesc||!productMainImage){showFormError("Please fill all required product fields before saving.");return!1}
if(!isValidUrl(productImage)||!isValidUrl(productMainImage)){showFormError("Please enter valid URLs for product images.");return!1}
const product={title:productTitle,prodImage:normalizeCloudinaryUrl(productMainImage),longDesc:productLongDesc,des:productDesc,type:productType,new:newProductFilter,image:normalizeCloudinaryUrl(productImage),conditions:[],id:generateId(10)};productsData=[...productsData,product];renderProducts();return!0}else if(saveType==="edit"){if(!editedProductId){showFormError("Cannot save changes without a selected product.");return!1}
productsData=productsData.map(product=>{if(product.id==editedProductId){return{...product,title:productTitle||product.title,prodImage:productMainImage?normalizeCloudinaryUrl(productMainImage):product.prodImage,longDesc:productLongDesc||product.longDesc,des:productDesc||product.des,type:productType||product.type,new:newProductFilter,image:productImage?normalizeCloudinaryUrl(productImage):product.image,conditions:product.conditions,id:product.id}}
return product});renderProducts();return!0}
return!1};const saveButton=document.querySelector(".submitProductButton");saveButton.addEventListener("click",()=>{if(saveProductData("add")){productAddingForm.classList.add("hidden");editSaveButton.classList.add("hidden");formContainer.classList.add("hidden")}});const openFormButton=document.querySelector(".addProductButton");const productAddingForm=document.querySelector(".addProductForm");openFormButton.addEventListener("click",()=>{productAddingForm.classList.remove("hidden");saveButton.classList.remove("hidden");formContainer.classList.remove("hidden")});const editSaveButton=document.querySelector(".editProductButton");editSaveButton.addEventListener("click",()=>{if(saveProductData("edit",editSaveButton.getAttribute("data-edit-id"))){productAddingForm.classList.add("hidden");formContainer.classList.add("hidden")}});const submitConditionButton=document.querySelector(".submitConditionButton");submitConditionButton.addEventListener("click",()=>{const condNameEl=document.querySelector(".condName");const condLinkEl=document.querySelector(".conditionLink");const condName=(condNameEl||{}).value||"";const condLink=(condLinkEl||{}).value||"";if(!condName.trim()||!condLink.trim()){showFormError("Please provide both condition name and link.");return}
if(!isValidUrl(condLink.trim())){showFormError("Please enter a valid condition link URL.");if(condLinkEl)condLinkEl.focus();return}
const productId=submitConditionButton.getAttribute("data-product-id");const product=productsData.find(p=>p.id==productId);if(!product){showFormError("Selected product not found. Please reopen the condition form.");return}
product.conditions=product.conditions||[];const newCondition={conditionName:condName.trim(),conditionLink:condLink.trim()};product.conditions.push(newCondition);conditionAddingForm.classList.add("hidden");renderProducts()});const fullView=document.querySelector(".fullView");const managementForm=document.querySelector(".addMangementMember");const managementSection=document.createElement("div");const managementTitle=document.createElement("h2");const addMemberButton=document.createElement("button");const managementList=document.createElement("ul");managementSection.classList.add("management-section");managementTitle.textContent="Members";addMemberButton.type="button";addMemberButton.textContent="Add Member";managementList.classList.add("management-list");managementSection.appendChild(managementTitle);managementSection.appendChild(addMemberButton);managementSection.appendChild(managementList);document.getElementById("members-page").appendChild(managementSection);const renderManagement=()=>{managementList.innerHTML="";mangementData.forEach(member=>{if(!member.id){member.id=generateId(10)}
const memberItem=document.createElement("li");memberItem.textContent=`${member.title} ${member.name} (${member.tag ||
      "management member"})`;memberItem.dataset.tag=member.tag||"management member";const deleteMemberButton=document.createElement("button");deleteMemberButton.type="button";deleteMemberButton.textContent="Delete";deleteMemberButton.setAttribute("data-member-id",member.id);deleteMemberButton.addEventListener("click",()=>{const memberId=deleteMemberButton.getAttribute("data-member-id");mangementData=mangementData.filter(m=>m.id!==memberId);renderManagement()});memberItem.appendChild(deleteMemberButton);managementList.appendChild(memberItem)})};addMemberButton.addEventListener("click",()=>{managementForm.classList.remove("hidden");formContainer.classList.remove("hidden")});const saveMemberButton=managementForm.querySelector(".saveMemberButton");saveMemberButton.addEventListener("click",()=>{const memberName=managementForm.querySelector(".memberName").value.trim();const memberEmail=managementForm.querySelector(".memberEmail").value.trim();const memberPhone=managementForm.querySelector(".memberPhone").value.trim();const memberTitle=managementForm.querySelector(".memberTitle").value.trim();const memberTag=managementForm.querySelector(".memberTag").value.trim();const memberImage=managementForm.querySelector(".memberImage").value.trim();const memberText=managementForm.querySelector(".memberText").value.trim();if(!memberName||!memberEmail||!memberPhone||!memberTitle||!memberImage||!memberText){showFormError("Please fill in all member fields before saving.");return}
if(!validateEmail(memberEmail)){showFormError("Please enter a valid email address.");return}
if(!isValidUrl(memberImage)){showFormError("Please enter a valid member image URL.");return}
const newMember={id:generateId(10),name:memberName,email:memberEmail,phone:memberPhone,title:memberTitle,tag:memberTag||"management member",image:memberImage,text:memberText};mangementData=[...mangementData,newMember];managementForm.classList.add("hidden");formContainer.classList.add("hidden");managementForm.reset();renderManagement()});renderManagement();const eventForm=document.querySelector(".addEventForm");const eventSection=document.createElement("div");const eventTitle=document.createElement("h2");const addEventButton=document.createElement("button");const eventList=document.createElement("ul");eventSection.classList.add("event-section");eventTitle.textContent="Events";addEventButton.type="button";addEventButton.textContent="Add Event";eventList.classList.add("event-list");eventSection.appendChild(eventTitle);eventSection.appendChild(addEventButton);eventSection.appendChild(eventList);document.getElementById("events-page").appendChild(eventSection);eventForm.innerHTML=`
  <label for="eventTitle">Title</label>
  <input type="text" id="eventTitle" class="eventTitle" placeholder="Event Title" required>
  <label for="eventDesc">Description</label>
  <textarea id="eventDesc" class="eventDesc" placeholder="Event Description" required></textarea>
  <div class="word-counter" data-for="eventDesc">0 / 70 words</div>
  <label for="eventDate">Date</label>
  <input type="text" id="eventDate" class="eventDate" placeholder="Event Date" required>
  <label for="eventShortDesc">Short Description</label>
  <input type="text" id="eventShortDesc" class="eventShortDesc" placeholder="Short Description" required>
  <div class="word-counter" data-for="eventShortDesc">0 / 15 words</div>
  <label for="eventYear">Year</label>
  <input type="text" id="eventYear" class="eventYear" placeholder="Year" required>
  <label>Event Filters</label>
  <div class="eventFiltersContainer"></div>
  <div class="eventCustomFiltersContainer"></div>
  <button type="button" class="addEventFilterInputButton">Add another filter</button>
  <label>Event Images (1-10 URLs)</label>
  <div class="eventImageUrlsContainer"></div>
  <button type="button" class="addEventImageUrlButton">Add another image URL</button>
  <button type="button" class="saveEventButton">Save New Event</button>
  <button type="button" class="cancelFormButton">Back</button>
`;eventForm.classList.add("hidden");function getEventFilters(event){if(!event)return[];if(Array.isArray(event.filters)&&event.filters.length>0){return event.filters.map(f=>String(f).trim()).filter(Boolean)}
if(Array.isArray(event.filter)&&event.filter.length>0){return event.filter.map(f=>String(f).trim()).filter(Boolean)}
if(typeof event.filter==="string"&&event.filter.trim()){return event.filter.split(",").map(f=>String(f).trim()).filter(Boolean)}
if(event.spez){return String(event.spez).split(",").map(f=>String(f).trim()).filter(Boolean)}
return[]}
function getUniqueEventFilters(events){const set=new Set();(events||[]).forEach(event=>{getEventFilters(event).forEach(filter=>{if(filter)set.add(filter);})});return Array.from(set).sort()}
function renderEventFilterCheckboxes(){const container=eventForm.querySelector(".eventFiltersContainer");if(!container)return;const filters=getUniqueEventFilters(eventData);container.innerHTML="";if(filters.length===0){const info=document.createElement("p");info.className="eventFiltersInfo";info.textContent="No filters available yet. Add one below.";container.appendChild(info)}
filters.forEach(filter=>{const label=document.createElement("label");label.className="eventFilterLabel";const checkbox=document.createElement("input");checkbox.type="checkbox";checkbox.className="eventFilterCheckbox";checkbox.value=filter;label.appendChild(checkbox);label.appendChild(document.createTextNode(filter));container.appendChild(label)})}
function addEventFilterOption(filterValue){const normalized=String(filterValue).trim();if(!normalized)return;const container=eventForm.querySelector(".eventFiltersContainer");if(!container)return;const existing=Array.from(container.querySelectorAll(".eventFilterCheckbox")).some(cb=>cb.value===normalized);if(existing)return;const label=document.createElement("label");label.className="eventFilterLabel";const checkbox=document.createElement("input");checkbox.type="checkbox";checkbox.className="eventFilterCheckbox";checkbox.value=normalized;checkbox.checked=!0;label.appendChild(checkbox);label.appendChild(document.createTextNode(normalized));container.appendChild(label)}
function createCustomEventFilterInput(value=""){const row=document.createElement("div");row.className="eventCustomFilterRow";const input=document.createElement("input");input.type="text";input.className="eventCustomFilterInput";input.placeholder="Filter name";input.value=value;row.appendChild(input);return row}
function addAnotherEventFilterInput(value=""){const container=eventForm.querySelector(".eventCustomFiltersContainer");if(!container)return;const row=createCustomEventFilterInput(value);container.appendChild(row);const input=row.querySelector(".eventCustomFilterInput");if(input)input.focus();}
function createEventImageUrlInput(value=""){const row=document.createElement("div");row.className="eventImageUrlRow";const input=document.createElement("input");input.type="url";input.className="eventImageUrlInput";input.placeholder="Image URL";input.value=value;const removeButton=document.createElement("button");removeButton.type="button";removeButton.className="removeEventImageUrlButton";removeButton.textContent="Remove";row.appendChild(input);row.appendChild(removeButton);return row}
function renderEventImageUrlInputs(){const container=eventForm.querySelector(".eventImageUrlsContainer");if(!container)return;container.innerHTML="";addEventImageUrlInput("")}
function addEventImageUrlInput(value=""){const container=eventForm.querySelector(".eventImageUrlsContainer");if(!container)return;const existingRows=container.querySelectorAll(".eventImageUrlRow").length;if(existingRows>=10){showFormError("You can add up to 10 images per event.");return}
const row=createEventImageUrlInput(value);container.appendChild(row);const input=row.querySelector(".eventImageUrlInput");if(input)input.focus();}
function gatherEventImageUrls(){const container=eventForm.querySelector(".eventImageUrlsContainer");if(!container)return[];const values=Array.from(container.querySelectorAll(".eventImageUrlInput")).map(input=>String(input.value||"").trim()).filter(Boolean);if(values.length===0){return[]}
const invalidValue=values.find(value=>!isValidUrl(value));if(invalidValue){showFormError("Please enter valid image URLs.");return null}
return values}
function gatherEventFormFilters(){const selected=Array.from(eventForm.querySelectorAll(".eventFilterCheckbox:checked")).map(checkbox=>String(checkbox.value).trim());const customFilterValues=Array.from(eventForm.querySelectorAll(".eventCustomFilterInput")).map(input=>String(input.value||"").trim()).filter(Boolean);customFilterValues.forEach(value=>{if(!selected.includes(value)){selected.push(value)}});return Array.from(new Set(selected)).filter(Boolean)}
function activateEventFilterForm(){renderEventFilterCheckboxes();const customFilterContainer=eventForm.querySelector(".eventCustomFiltersContainer");if(customFilterContainer){customFilterContainer.innerHTML="";addAnotherEventFilterInput("")}}
function clearEventFormFilters(){const container=eventForm.querySelector(".eventFiltersContainer");if(container){container.querySelectorAll(".eventFilterCheckbox").forEach(cb=>(cb.checked=!1))}
const customFilterContainer=eventForm.querySelector(".eventCustomFiltersContainer");if(customFilterContainer){customFilterContainer.innerHTML="";addAnotherEventFilterInput("")}}
const addEventFilterInputButton=eventForm.querySelector(".addEventFilterInputButton");if(addEventFilterInputButton){addEventFilterInputButton.addEventListener("click",()=>{addAnotherEventFilterInput("")})}
const addEventImageUrlButton=eventForm.querySelector(".addEventImageUrlButton");if(addEventImageUrlButton){addEventImageUrlButton.addEventListener("click",()=>{addEventImageUrlInput("")})}
if(eventForm){eventForm.addEventListener("click",event=>{const removeButton=event.target.closest(".removeEventImageUrlButton");if(!removeButton)return;const row=removeButton.closest(".eventImageUrlRow");if(row){row.remove()}})}
function updateEventWordCounters(){const counters=eventForm.querySelectorAll(".word-counter");counters.forEach(counter=>{const targetClass=counter.getAttribute("data-for");const input=eventForm.querySelector(`.${targetClass}`);if(!input)return;const count=countWords(input.value);const limit=targetClass==="eventDesc"?70:15;const isOver=count>limit;counter.textContent=`${count} / ${limit} words`;counter.classList.toggle("over-limit",isOver)})}
if(eventForm){eventForm.addEventListener("input",event=>{if(event.target.classList.contains("eventDesc")||event.target.classList.contains("eventShortDesc")){updateEventWordCounters()}})}
const renderEvents=()=>{eventList.innerHTML="";eventData.forEach(event=>{const eventItem=document.createElement("li");eventItem.innerHTML=`<strong>${event.title}</strong> - ${event.desc} - ${event.date}`;const deleteEventButton=document.createElement("button");deleteEventButton.type="button";deleteEventButton.textContent="Delete";deleteEventButton.setAttribute("data-event-title",event.title);deleteEventButton.addEventListener("click",()=>{const eventTitle=deleteEventButton.getAttribute("data-event-title");eventData=eventData.filter(e=>e.title!==eventTitle);renderEvents()});eventItem.appendChild(deleteEventButton);eventList.appendChild(eventItem)})};addEventButton.addEventListener("click",()=>{activateEventFilterForm();renderEventImageUrlInputs();eventForm.classList.remove("hidden");formContainer.classList.remove("hidden")});const saveEventButton=eventForm.querySelector(".saveEventButton");saveEventButton.addEventListener("click",()=>{const eventTitle=String(eventForm.querySelector(".eventTitle")?.value||"").trim();const eventDesc=String(eventForm.querySelector(".eventDesc")?.value||"").trim();const eventDate=String(eventForm.querySelector(".eventDate")?.value||"").trim();const eventShortDesc=String(eventForm.querySelector(".eventShortDesc")?.value||"").trim();const eventYear=String(eventForm.querySelector(".eventYear")?.value||"").trim();const eventLink=String(eventForm.querySelector(".eventLink")?.value||"").trim();const selectedFilters=gatherEventFormFilters();const eventImages=gatherEventImageUrls();if(eventImages===null){return}
if(!eventTitle||!eventDesc||!eventDate||!eventShortDesc||!eventYear){showFormError("Please fill all event fields before saving.");return}
const descWordCount=countWords(eventDesc);const shortDescWordCount=countWords(eventShortDesc);if(descWordCount>70){showFormError("Event description must be 70 words or fewer.");return}
if(shortDescWordCount>15){showFormError("Short description must be 15 words or fewer.");return}
if(isNaN(Number(eventYear))||!Number.isInteger(Number(eventYear))){showFormError("Please enter a valid numeric year for the event.");return}
const parsedDate=new Date(eventDate);if(isNaN(parsedDate.getTime())){showFormError("Please enter a valid event date.");return}
if(eventLink&&!isValidUrl(eventLink)){showFormError("Please enter a valid URL for the event link.");return}
if(eventImages.length===0){showFormError("Please add at least one image URL for the event.");return}
const newEvent={title:eventTitle,desc:eventDesc,date:eventDate,"short-desc":eventShortDesc,year:eventYear,filters:selectedFilters,link:eventLink,images:eventImages};if(newEvent.filters.length>0){newEvent.spez=newEvent.filters[0]}
eventData=[...eventData,newEvent];eventForm.classList.add("hidden");formContainer.classList.add("hidden");eventForm.reset();clearEventFormFilters();renderEventImageUrlInputs();renderEvents()});renderEvents();const newEventForm=document.querySelector(".addNewEventForm");const newEventSection=document.createElement("div");const newEventTitle=document.createElement("h2");const addNewEventButton=document.createElement("button");const newEventList=document.createElement("ul");newEventSection.classList.add("new-event-section");newEventTitle.textContent="New Events";addNewEventButton.type="button";addNewEventButton.textContent="Add New Event";newEventList.classList.add("new-event-list");newEventSection.appendChild(newEventTitle);newEventSection.appendChild(addNewEventButton);newEventSection.appendChild(newEventList);document.getElementById("new-events-page").appendChild(newEventSection);newEventForm.innerHTML=`
  <label for="newEventImg1">Image 1 URL</label>
  <input type="text" id="newEventImg1" class="newEventImg1" placeholder="Image 1 URL" required>
  <label for="newEventImg2">Image 2 URL</label>
  <input type="text" id="newEventImg2" class="newEventImg2" placeholder="Image 2 URL" required>
  <button type="button" class="saveNewEventButton">Save New Event</button>
  <button type="button" class="cancelFormButton">Back</button>
`;newEventForm.classList.add("hidden");const renderNewEvents=()=>{newEventList.innerHTML="";newEventData.forEach((event,index)=>{const eventItem=document.createElement("li");eventItem.innerHTML=`<img src="${normalizeCloudinaryUrl(
      event.img1
    )}" loading="lazy" alt="Img1" style="width:50px;"> <img src="${normalizeCloudinaryUrl(
      event.img2
    )}" loading="lazy" alt="Img2" style="width:50px;">`;const deleteNewEventButton=document.createElement("button");deleteNewEventButton.type="button";deleteNewEventButton.textContent="Delete";deleteNewEventButton.setAttribute("data-index",index);deleteNewEventButton.addEventListener("click",()=>{const idx=parseInt(deleteNewEventButton.getAttribute("data-index"));newEventData=newEventData.filter((_,i)=>i!==idx);renderNewEvents()});eventItem.appendChild(deleteNewEventButton);newEventList.appendChild(eventItem)})};addNewEventButton.addEventListener("click",()=>{newEventForm.classList.remove("hidden");formContainer.classList.remove("hidden")});const saveNewEventButton=newEventForm.querySelector(".saveNewEventButton");saveNewEventButton.addEventListener("click",()=>{const img1=newEventForm.querySelector(".newEventImg1").value.trim();const img2=newEventForm.querySelector(".newEventImg2").value.trim();if(!img1||!img2){showFormError("Please provide both new event image URLs.");return}
if(!isValidUrl(img1)||!isValidUrl(img2)){showFormError("Please enter valid URLs for both new event images.");return}
const newEvent={img1:normalizeCloudinaryUrl(img1),img2:normalizeCloudinaryUrl(img2)};newEventData=[...newEventData,newEvent];newEventForm.classList.add("hidden");formContainer.classList.add("hidden");newEventForm.reset();renderNewEvents()});renderNewEvents();const navButtons=document.querySelectorAll(".nav-btn");const pages=document.querySelectorAll(".page");navButtons.forEach(button=>{button.addEventListener("click",()=>{navButtons.forEach(btn=>btn.classList.remove("active"));pages.forEach(page=>page.classList.remove("active"));button.classList.add("active");const pageId=button.getAttribute("data-page")+"-page";document.getElementById(pageId).classList.add("active")})});const renderProducts=()=>{fullView.innerHTML="";productsData.forEach(item=>{const compCard=document.createElement("div");compCard.classList.add("compCard");const compTitle=document.createElement("h2");compTitle.textContent=item.title;const conditionsSection=document.createElement("div");conditionsSection.className="conditions-section";conditionsSection.style.display="none";const conditionsList=document.createElement("ul");conditionsList.className="conditions-list";(item.conditions||[]).forEach(condition=>{const conditionItem=document.createElement("li");conditionItem.textContent=condition.conditionName||"";const deleteConditionButton=document.createElement("button");deleteConditionButton.textContent="Delete Condition";deleteConditionButton.setAttribute("data-cond-name",condition.conditionName||"");deleteConditionButton.setAttribute("data-product-id",item.id?item.id:1);deleteConditionButton.addEventListener("click",()=>{const condName=deleteConditionButton.getAttribute("data-cond-name");const productId=deleteConditionButton.getAttribute("data-product-id");const product=productsData.find(p=>p.id==productId);if(product){product.conditions=(product.conditions||[]).filter(c=>c.conditionName!==condName);renderProducts()}});conditionItem.appendChild(deleteConditionButton);conditionsList.appendChild(conditionItem)});const conditionsV2List=document.createElement("ul");conditionsV2List.className="conditionsV2-list";(item.conditionsV2||[]).forEach(cv2=>{const li=document.createElement("li");li.textContent=`${cv2.name || ""} ${cv2.date
        ? "(" + cv2.date + ")"
        : ""}`;const del=document.createElement("button");del.type="button";del.textContent="Delete V2";del.addEventListener("click",()=>{item.conditionsV2=(item.conditionsV2||[]).filter(c=>c.id!==cv2.id);renderProducts()});li.appendChild(del);conditionsV2List.appendChild(li)});if(conditionsList.children.length>0){conditionsSection.appendChild(conditionsList)}
if(conditionsV2List.children.length>0){conditionsSection.appendChild(conditionsV2List)}
const showConditionsButton=document.createElement("button");showConditionsButton.textContent="Show Medical clinical cases";showConditionsButton.addEventListener("click",()=>{const isHidden=conditionsSection.style.display==="none";conditionsSection.style.display=isHidden?"block":"none";showConditionsButton.textContent=isHidden?"Hide Medical clinical cases":"Show Medical clinical cases"});const editProductButton=document.createElement("button");editProductButton.textContent="Edit Product";editProductButton.id=item.id;editProductButton.addEventListener("click",()=>{productAddingForm.classList.remove("hidden");editSaveButton.classList.remove("hidden");saveButton.classList.add("hidden");formContainer.classList.remove("hidden");editSaveButton.setAttribute("data-edit-id",editProductButton.id)});const deleteProductButton=document.createElement("button");deleteProductButton.textContent="Delete Product";deleteProductButton.addEventListener("click",()=>{productsData=productsData.filter(product=>product.id!=item.id);renderProducts()});const conditionsButton=document.createElement("button");conditionsButton.textContent="add condition";conditionsButton.setAttribute("data-product-id",item.id);conditionsButton.addEventListener("click",()=>{conditionAddingForm.classList.remove("hidden");formContainer.classList.remove("hidden");submitConditionButton.setAttribute("data-product-id",item.id)});const conditionsV2Button=document.createElement("button");conditionsV2Button.textContent="add condition v2";conditionsV2Button.setAttribute("data-product-id",item.id);conditionsV2Button.addEventListener("click",()=>{if(!conditionV2FormEl)return;conditionV2FormEl.classList.remove("hidden");formContainer.classList.remove("hidden");const pid=conditionV2FormEl.querySelector(".condV2ProductId");if(pid)pid.value=item.id;const images=conditionV2FormEl.querySelector(".condV2Images");if(images&&images.children.length===0)
images.appendChild(createCondV2ImageRow());});const actionsWrapper=document.createElement("div");actionsWrapper.className="compCard-actions";actionsWrapper.appendChild(conditionsButton);actionsWrapper.appendChild(conditionsV2Button);actionsWrapper.appendChild(showConditionsButton);actionsWrapper.appendChild(deleteProductButton);actionsWrapper.appendChild(editProductButton);const contentWrapper=document.createElement("div");contentWrapper.className="compCard-content";contentWrapper.appendChild(compTitle);contentWrapper.appendChild(conditionsSection);compCard.appendChild(actionsWrapper);compCard.appendChild(contentWrapper);fullView.appendChild(compCard)})};renderProducts();function showToast(message,type="success"){toast.textContent=message;toast.classList.remove("hidden");toast.classList.add(`${type === "error" ? "failure-toast" : "sucess-toast"}`);setTimeout(()=>{toast.className="hidden"},3000)}
function loadAndDisplayDataForUser(){console.log("loading data for user...");const docRef=doc(db,"websiteContent","main");let newdata=onSnapshot(docRef,doc=>{if(doc.exists()){data=doc.data();console.log(data);async function generatemanagmentCardsDependsOnRespnse(){let response=data.management||[];console.log(response);let swiperWrapper=document.querySelector(".swiper-wrapper");if(!swiperWrapper)return;swiperWrapper.innerHTML="";for(const val of response){let card=document.createElement("div");card.classList.add("card");card.classList.add("swiper-slide");card.dataset.tag=val.tag||"management member";swiperWrapper.appendChild(card);let cardImage=document.createElement("div");cardImage.classList.add("card__image");let cardImageImg=document.createElement("img");cardImageImg.setAttribute("src",normalizeCloudinaryUrl(val.image));cardImageImg.setAttribute("loading","lazy");cardImage.appendChild(cardImageImg);card.appendChild(cardImage);let cardContent=document.createElement("div");cardContent.classList.add("card__content");let cardTitle=document.createElement("span");cardTitle.classList.add("card__title");cardTitle.innerHTML=val.title;cardContent.appendChild(cardTitle);let cardName=document.createElement("span");cardName.classList.add("card__name");cardName.innerHTML=val.name;cardContent.appendChild(cardName);let cardText=document.createElement("p");cardText.classList.add("card__text");cardText.innerHTML=val.text;cardContent.appendChild(cardText);let cardPhone=document.createElement("p");cardPhone.classList.add("card__phone");cardPhone.innerHTML=`Phone: ${val.phone}`;cardContent.appendChild(cardPhone);let cardEmail=document.createElement("p");cardEmail.classList.add("card__email");cardEmail.innerHTML=`Email: ${val.email}`;cardContent.appendChild(cardEmail);card.appendChild(cardContent)}}
generatemanagmentCardsDependsOnRespnse();function resetSwiperSlideStyles(filteredManagementCards){const allSlides=Array.from(document.querySelectorAll(".swiper-wrapper .swiper-slide"));allSlides.forEach(s=>{s.className="card swiper-slide";s.removeAttribute("aria-label");s.style.transform="";s.style.opacity="";s.style.filter="";s.style.zIndex="";s.style.transitionDuration=""});let visibleCards;if(filteredManagementCards&&filteredManagementCards.length){visibleCards=Array.from(filteredManagementCards)}else{visibleCards=allSlides.filter(s=>s.style.display!=="none")}
if(!visibleCards||visibleCards.length===0)return;const total=visibleCards.length;visibleCards.forEach((card,idx)=>{const i=idx+1;const translateZ=-25-idx*300;const zIndex=2-i;const transform=`translate3d(0px, 0px, ${translateZ}px) rotateX(0deg) rotateY(0deg) scale(1)`;card.className="card swiper-slide";card.setAttribute("aria-label",`${i} / ${total}`);card.style.transitionDuration="0ms";card.style.transform=transform;card.style.zIndex=String(zIndex);if(i===1){card.classList.add("swiper-slide-visible","swiper-slide-fully-visible","swiper-slide-active")}else if(i===2){card.classList.add("swiper-slide-visible","swiper-slide-next")}})}
if(window.swiper&&typeof window.swiper.update==="function"){window.swiper.update();window.swiper.slideTo(0);resetSwiperSlideStyles()}
const managementFilterBtns=document.querySelectorAll(".management-filter-btn");if(managementFilterBtns&&managementFilterBtns.length){managementFilterBtns.forEach(btn=>{btn.addEventListener("click",()=>{managementFilterBtns.forEach(b=>b.classList.remove("active"));btn.classList.add("active");const filter=btn.dataset.filter;const members=data&&data.management?data.management:[];const visibleMembers=filter==="all"?members:members.filter(m=>(m.tag||"management member")===filter);const slidesHTML=visibleMembers.map(m=>{const tagAttr=m.tag?`data-tag="${m.tag}"`:`data-tag="management member"`;const img=normalizeCloudinaryUrl(m.image||"");const title=m.title||"";const name=m.name||"";const text=m.text||"";const phone=m.phone||"";const email=m.email||"";return `
                  <div class="card swiper-slide" ${tagAttr}>
                    <div class="card__image"><img src="${img}" loading="lazy" alt="${name}"></div>
                    <div class="card__content">
                      <span class="card__title">${title}</span>
                      <span class="card__name">${name}</span>
                      <p class="card__text">${text}</p>
                      <p class="card__phone">Phone: ${phone}</p>
                      <p class="card__email">Email: ${email}</p>
                      <button class="card__btn">VIEW MORE</button>
                    </div>
                  </div>`});if(window.swiper&&typeof window.swiper.removeAllSlides==="function"&&typeof window.swiper.appendSlide==="function"){try{window.swiper.removeAllSlides()}catch(e){}
try{window.swiper.appendSlide(slidesHTML)}catch(e){slidesHTML.forEach(h=>window.swiper.appendSlide(h))}
window.swiper.update();window.swiper.slideTo(0);const newSlides=Array.from(document.querySelectorAll(".swiper-wrapper .swiper-slide"));if(typeof resetSwiperSlideStyles==="function")
resetSwiperSlideStyles(newSlides);}else{const wrapper=document.querySelector(".swiper-wrapper");if(wrapper){wrapper.innerHTML=slidesHTML.join("");if(typeof resetSwiperSlideStyles==="function"){const newSlides=Array.from(document.querySelectorAll(".swiper-wrapper .swiper-slide"));resetSwiperSlideStyles(newSlides)}
if(window.swiper&&typeof window.swiper.update==="function"){window.swiper.update();window.swiper.slideTo(0)}}}})});const allBtn=document.querySelector('.management-filter-btn[data-filter="all"]');if(allBtn)allBtn.classList.add("active");}
let cards=[];async function generateproductsCardsDependsOnRespnse(contType,contType2){let response=data.products;console.log(response);let typesArr=[];let val;let type;function capitalizeWord(w){if(!w)return w;return w.charAt(0).toUpperCase()+w.slice(1).toLowerCase()}
function capitalizeWords(s){return s.trim().split(/\s+/).map(capitalizeWord).join(" ")}
function getClassTokensFromType(s){if(!s||typeof s!=="string")return[];const parts=s.trim().split(/\s+/);if(parts.length===1)return[capitalizeWord(parts[0])];if(parts.length===2)
return[`${capitalizeWord(parts[0])}-${capitalizeWord(parts[1])}`];return parts.map(capitalizeWord)}
const seen=new Set();for(val of response){if(!val||!val.type)continue;const rawTypes=val.type.includes(",")?val.type.split(",").map(t=>t.trim()):[val.type.trim()];rawTypes.forEach(raw=>{if(!raw)return;const display=capitalizeWords(raw);const value=getClassTokensFromType(raw)[0];if(!seen.has(value)){seen.add(value);typesArr.push({display,value})}})}
console.log(typesArr);function generateSelectOps(){const sel=document.querySelector(".section-options ul select");if(!sel)return;typesArr.forEach(item=>{let selectOption=document.createElement("option");selectOption.textContent=item.display;selectOption.value=item.value;sel.appendChild(selectOption)})}
generateSelectOps();for(val of response){let cardsConatiner=document.querySelector(".cards");let card=document.createElement("div");card.classList.add("product-card");card.classList.add("all");console.log(val);if(val.type.includes(",")){val.type.split(",").forEach(elRaw=>{const tokens=getClassTokensFromType(elRaw);tokens.forEach(t=>card.classList.add(t))})}else{const tokens=getClassTokensFromType(val.type);tokens.forEach(t=>card.classList.add(t))}
let productImagecon=document.createElement("div");productImagecon.classList.add("product-image");card.appendChild(productImagecon);let productImage=document.createElement("img");productImage.setAttribute("src",normalizeCloudinaryUrl(val.image));productImage.setAttribute("loading","lazy");productImagecon.appendChild(productImage);let productName=document.createElement("div");productName.classList.add("product-name");card.appendChild(productName);let productNameText=document.createElement("div");productNameText.innerHTML=val.title;productName.appendChild(productNameText);let productDes=document.createElement("div");productDes.classList.add("product-text");productDes.textContent=val.des;card.appendChild(productDes);let viewMoreButton=document.createElement("button");viewMoreButton.textContent=`VIEW MORE`;viewMoreButton.name=val.title;viewMoreButton.classList.add("moreButton");card.appendChild(viewMoreButton);if(val.new===!0){let newSpan=document.createElement("span");newSpan.classList.add("new");newSpan.style.padding="7px";newSpan.textContent=`NEW`;card.appendChild(newSpan);card.classList.add("new")}
cardsConatiner.appendChild(card);cards.push(card)}}
generateproductsCardsDependsOnRespnse(contType,contType2);let productLis=document.querySelectorAll(".product ul li");let selectOps=document.querySelector(".product ul select");let productCardFilterButton=document.querySelector(".product-card-filter");productLis.forEach(li=>{li.addEventListener("click",()=>{productLis.forEach(el=>{el.classList.remove("active")});li.classList.add("active");cards.forEach(card=>{card.style.display="none"});selectOps.selectedIndex=0;selectOps.style.display="none";document.querySelectorAll(li.dataset.cat).forEach(e=>(e.style.display="block"))})});productCardFilterButton.addEventListener("click",()=>{selectOps.style.display="block"});selectOps.addEventListener("change",()=>{let op=selectOps.value;console.log(op);productLis.forEach(el=>{el.classList.remove("active")});productLis[2].classList.add("active");cards.forEach(card=>{card.style.display="none"});document.querySelectorAll(`.${op}`).forEach(e=>{e.style.display="block"})});const moreMainView=document.querySelector(".moreMainView");const moreProductName=document.querySelector(".moreProductName");const moreProductDesc=document.querySelector(".moreProductDesc p");const moreProductImage=document.querySelector(".moreProductDesc img");if(moreProductImage)moreProductImage.setAttribute("loading","lazy");const outerCond=document.querySelector(".outerCond");const outerCondImages1=document.querySelectorAll(".outerCondImages1 img");const outerCondImages2=document.querySelectorAll(".outerCondImages2 img");const goBackButton=document.querySelector(".goBackButton");const allCondMainView=document.querySelector(".allConditionsSection");let outerCondGallery=document.querySelector(".outerCondGallery");let cond;let condImg;let val;const viewMoreButton=document.querySelectorAll(".moreButton");let showViewMoreProductSection=product=>{let choosenProductButton;viewMoreButton.forEach(button=>{if(button.name==product){choosenProductButton=button}});bringProductData(product,choosenProductButton);hideEveryThingElse([moreMainView]);moreMainView.classList.remove("hidden");moreMainView.style.left="0";goBackButton.classList.remove("hidden");window.scrollTo({top:0,behavior:"smooth"})};const slugifyProductTitle=title=>String(title||"").trim().toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"");const openProductFromHash=()=>{const hash=window.location.hash||"";if(!hash.startsWith("#product-"))return;const productSlug=hash.replace(/^#product-/,"").toLowerCase().trim();if(!data||!Array.isArray(data.products))return;const matchedProduct=data.products.find(product=>{const titleSlug=slugifyProductTitle(product.title);return titleSlug===productSlug});if(matchedProduct){showViewMoreProductSection(matchedProduct.title)}};window.addEventListener("Load",()=>{console.log("Current hash:",window.location.hash);openProductFromHash()});window.addEventListener("hashchange",()=>{openProductFromHash()});let choosenProduct;viewMoreButton.forEach(button=>{button.addEventListener("click",()=>{hideEveryThingElse([moreMainView]);bringProductData(button.name,button);moreMainView.classList.remove("hidden");moreMainView.style.left="0";goBackButton.classList.remove("hidden");window.location.hash=`#product-${button.name.replace(
              /\s+/g,
              "-"
            ).toLowerCase()}`;window.scrollTo({top:0,behavior:"smooth"})})});let hideEveryThingElse=arrayOfNonHidden=>{let everyThing=document.body.children;Array.from(everyThing).forEach(el=>{el.classList.contains("hidden")?"":el.classList.add("hidden")});arrayOfNonHidden.forEach(el=>{el.classList.remove("hidden")});document.querySelector("header").classList.remove("hidden");document.querySelector(".slide-nav").classList.remove("hidden");document.querySelector(".gtranslate_wrapper").classList.remove("hidden")};let bringProductData=(product,button)=>{for(val of data.products){if(val.title===product){console.log("right");choosenProduct=val;moreProductName.innerText=choosenProduct.title;moreProductDesc.innerText=choosenProduct.longDesc;moreProductImage.src=normalizeCloudinaryUrl(choosenProduct.prodImage);const conds=choosenProduct.conditions||[];const condsV2=choosenProduct.conditionsV2||[];if(outerCondGallery)outerCondGallery.innerHTML="";if(conds.length>0||condsV2.length>0){const viewAllCondButton=document.createElement("button");viewAllCondButton.classList.add("viewMoreCondButton");viewAllCondButton.textContent="see more Medical clinical cases";viewAllCondButton.type="button";outerCondGallery.appendChild(viewAllCondButton);viewAllCondButton.addEventListener("click",()=>{allCondMainView.innerHTML="";const backToMoreBtn=document.createElement("button");backToMoreBtn.type="button";backToMoreBtn.classList.add("goBackToMoreButton");backToMoreBtn.textContent="go back to product description";const conditionsHeader=document.createElement("h1");conditionsHeader.classList.add("conditions-header");conditionsHeader.innerText=`All Medical clinical casesof ${choosenProduct.title}`;allCondMainView.appendChild(conditionsHeader);const list=document.createElement("div");list.classList.add("conditions-list");conds.forEach(c=>{const row=document.createElement("div");row.classList.add("condition-row");const nameSpan=document.createElement("span");nameSpan.classList.add("condition-text");nameSpan.textContent=c.conditionName||"";const openBtn=document.createElement("button");openBtn.classList.add("condition-open-button");openBtn.type="button";openBtn.textContent="Open";openBtn.addEventListener("click",()=>{const link=c.conditionLink||"#";window.open(link,"_blank","noopener,noreferrer")});row.appendChild(nameSpan);row.appendChild(openBtn);list.appendChild(row)});allCondMainView.classList.remove("hidden");list.classList.add("condition-v2-list");condsV2.forEach(c=>{const card=document.createElement("div");card.classList.add("condition-v2-card");const title=document.createElement("h2");title.textContent=c.name||"";card.appendChild(title);if(c.date){const timeEl=document.createElement("time");timeEl.setAttribute("datetime",c.date);timeEl.textContent=c.date;card.appendChild(timeEl)}
if(c.text){const p=document.createElement("p");p.textContent=c.text;card.appendChild(p)}
if(Array.isArray(c.images)&&c.images.length){const gallery=document.createElement("div");gallery.classList.add("condition-v2-gallery");c.images.forEach(img=>{const fig=document.createElement("figure");const im=document.createElement("img");im.src=normalizeCloudinaryUrl(img.url||"");im.loading="lazy";im.decoding="async";im.alt=img.alt||`${choosenProduct.title} - ${c.name || ""}`;fig.appendChild(im);gallery.appendChild(fig)});card.appendChild(gallery)}
list.appendChild(card)});allCondMainView.appendChild(list);allCondMainView.classList.remove("hidden");moreMainView.classList.add("hidden");backToMoreBtn.addEventListener("click",()=>{allCondMainView.classList.add("hidden");allCondMainView.innerHTML="";moreMainView.classList.remove("hidden")});allCondMainView.appendChild(backToMoreBtn)});if(button.classList.contains("hidden"))
button.classList.remove("hidden");}else{if(outerCondGallery)outerCondGallery.innerHTML=""}}}};goBackButton.addEventListener("click",()=>{moreMainView.classList.add("hidden");if(!allCondMainView.classList.contains("hidden")){allCondMainView.classList.add("hidden");allCondMainView.innerHTML=""}
const outerGallery=document.querySelector(".outerCondGallery");if(outerGallery)outerGallery.innerHTML="";document.querySelectorAll(".hidden").forEach(el=>el.classList.remove("hidden"));moreMainView.classList.add("hidden");allCondMainView.classList.add("hidden");goBackButton.classList.add("hidden");overlayBackground.classList.add("hidden");loginSection.classList.add("hidden");adminPanel.classList.add("hidden");toast.classList.add("hidden");goBackButton.classList.add("hidden")});let eventCard=document.querySelectorAll(".eventCard");let eventHeader=document.querySelector(".left-sid-cont h1");let eventParagraph=document.querySelector(".left-sid-cont p");let linkEvent=document.querySelector(".left-sid-cont button a");let cardCont=document.querySelector(".cards-container");let evenTdotsCont=document.querySelector(".cards-container .spans");let smallEventArr=[];let beforButton=document.querySelector(".spans #before, .spans #befor");let afterbutton=document.querySelector(".spans #after");let eventMainCont=document.querySelector(".Events-cont");let decorationText=document.querySelector(".decoration-text");let addAncherTobutton=document.querySelectorAll(".eventCard div:nth-child(2) button");let openEventImageModal=(imageUrl,title)=>{if(!imageUrl)return;let overlay=document.querySelector(".event-image-lightbox");if(!overlay){overlay=document.createElement("div");overlay.className="event-image-lightbox";overlay.innerHTML=`
              <button type="button" class="event-image-lightbox__close" aria-label="Close image">×</button>
              <div class="event-image-lightbox__frame">
                <img src="" alt="" />
              </div>
            `;document.body.appendChild(overlay)}
const img=overlay.querySelector("img");const closeButton=overlay.querySelector(".event-image-lightbox__close");if(img){img.src=normalizeCloudinaryUrl(imageUrl);img.alt=title||"Event image"}
const closeModal=()=>{overlay.classList.remove("show");document.body.classList.remove("event-lightbox-open")};closeButton?.addEventListener("click",closeModal,{once:!0});overlay.addEventListener("click",event=>{if(event.target===overlay){closeModal()}},{once:!0});document.body.classList.add("event-lightbox-open");overlay.classList.add("show")};let getEventImages=event=>{if(!event)return[];const candidates=[Array.isArray(event.images)?event.images:null,Array.isArray(event.image)?event.image:null,Array.isArray(event.img)?event.img:null].filter(Boolean);if(candidates.length>0){return candidates.flat().filter(Boolean)}
return[event.img1,event.img2,event.imageUrl,event.imageurl].filter(Boolean)};let renderEventGallery=event=>{if(!decorationText)return;const images=getEventImages(event);decorationText.innerHTML="";decorationText.style.display="flex";decorationText.style.flexDirection="column";decorationText.style.gap="10px";if(images.length===0){const emptyText=document.createElement("p");emptyText.textContent="No images available for this event.";decorationText.appendChild(emptyText);return}
const gallery=document.createElement("div");gallery.className="event-gallery-grid";images.forEach(imageUrl=>{const thumbButton=document.createElement("button");thumbButton.type="button";thumbButton.className="event-gallery-thumb";thumbButton.addEventListener("click",()=>{openEventImageModal(imageUrl,event?.title||"Event image")});const image=document.createElement("img");image.src=normalizeCloudinaryUrl(imageUrl);image.alt=`${event?.title || "Event"} image`;image.loading="lazy";image.style.width="100%";image.style.height="70px";image.style.objectFit="cover";image.style.borderRadius="6px";thumbButton.appendChild(image);gallery.appendChild(thumbButton)});decorationText.appendChild(gallery)};let resetClick=()=>{let card=document.querySelectorAll(".cards-container .eventCard");Array.from(card).forEach(ele=>{ele.children[0].classList.contains("clicked")?ele.children[0].classList.remove("clicked"):ele})};let eventPageIndex=0;let eventPageGroups=[];let updateEventCarouselView=()=>{const track=document.querySelector(".cards-container .small-cont");if(!track||!track.children.length)return;const pages=Array.from(track.children);pages.forEach((page,index)=>{page.classList.toggle("active",index===eventPageIndex);page.classList.toggle("is-left",index<eventPageIndex);page.classList.toggle("is-right",index>eventPageIndex)});const activePage=pages[eventPageIndex]||pages[0];if(beforButton){beforButton.classList.toggle("disabled",eventPageIndex===0)}
if(afterbutton){afterbutton.classList.toggle("disabled",eventPageIndex>=eventPageGroups.length-1)}};let createEventCard=(currentEvent,cardIndex)=>{let cardDiv=document.createElement("div");cardDiv.classList.add("eventCard");cardDiv.dataset.num=cardIndex;let date=String(currentEvent?.date||"").split(" ");let dateDiv=document.createElement("div");dateDiv.classList.add("old-date");let day=document.createElement("h1");day.classList.add("day");day.appendChild(document.createTextNode(date[0]));let mounth=document.createElement("p");mounth.appendChild(document.createTextNode(date[1]));mounth.classList.add("mounth");dateDiv.appendChild(day);dateDiv.appendChild(mounth);cardDiv.appendChild(dateDiv);let secondDiv=document.createElement("div");let title=document.createElement("h1");title.classList.add("title");title.append(document.createTextNode(currentEvent?.title||""));let eventText=document.createElement("P");eventText.classList.add("event-text");eventText.appendChild(document.createTextNode(currentEvent?.["short-desc"]||""));let eventButton=document.createElement("button");eventButton.classList.add("event-button");let buttonAn=document.createElement("a");buttonAn.append(document.createTextNode("show more"));eventButton.setAttribute("data-set-num",cardIndex)
eventButton.append(buttonAn);eventButton.addEventListener("click",(e)=>{resetClick();eventHeader.innerHTML=currentEvent?.title||"";eventParagraph.innerHTML=currentEvent?.desc||"";linkEvent.href=currentEvent?.link||"#";dateDiv.classList.add("clicked");renderEventGallery(currentEvent);e.target.getAttribute("data-set-num")==0?"":document.querySelector(".left-sid-cont #desc").scrollIntoView({behavior:"smooth",block:"start"})});secondDiv.appendChild(title);secondDiv.appendChild(eventText);secondDiv.appendChild(eventButton);cardDiv.appendChild(secondDiv);return cardDiv};let makeCard=arr=>{document.querySelector(".cards-container .small-cont")?.remove();let evenTcontDiv=document.createElement("div");evenTcontDiv.classList.add("small-cont");evenTcontDiv.setAttribute("aria-live","polite");eventPageGroups=[];for(let eventCounter=0;eventCounter<arr.length;eventCounter+=3){const pageEvents=arr.slice(eventCounter,eventCounter+3);const pageDiv=document.createElement("div");pageDiv.classList.add("small-cont__page");pageEvents.forEach((currentEvent,index)=>{pageDiv.appendChild(createEventCard(currentEvent,eventCounter+index))});evenTcontDiv.appendChild(pageDiv);eventPageGroups.push(pageEvents)}
cardCont.appendChild(evenTcontDiv);eventPageIndex=0;requestAnimationFrame(()=>{updateEventCarouselView();if(evenTcontDiv.querySelector(".event-button")){evenTcontDiv.querySelector(".event-button").click()}})};let controlCenter=(num,arr)=>{if(num===0){makeCard(arr)}};if(beforButton&&afterbutton){beforButton.addEventListener("click",()=>{if(eventPageIndex>0){eventPageIndex-=1;updateEventCarouselView()}});afterbutton.addEventListener("click",()=>{if(eventPageIndex<eventPageGroups.length-1){eventPageIndex+=1;updateEventCarouselView()}})}
let select=document.createElement("select");let select2=document.createElement("select");let selectAndButtonMaker=arr=>{select.name="Speizelation";select.classList.add("select");select.setAttribute("id","select1");let selectButton=document.createElement("button");selectButton.classList.add("selectButton");selectButton.innerText="Filter Events";select2.name="year";select2.classList.add("select2");select2.setAttribute("id","select2");let selectCont=document.createElement("div");selectCont.append(select);selectCont.append(select2);selectCont.append(selectButton);selectCont.classList.add("selectCont");cardCont.prepend(selectCont)};let spezOptionMaker=set=>{let selectQuer=document.querySelector("select.select");for(let i=0;i<set.size;i++){let optionCont=document.createElement("option");optionCont.value=Array.from(set)[i];optionCont.appendChild(document.createTextNode(Array.from(set)[i]));selectQuer.append(optionCont)}};let yearOptionMaker=set=>{let selectQuer=document.querySelector("select.select2");for(let i=0;i<set.size;i++){let optionCont=document.createElement("option");optionCont.value=Array.from(set)[i];optionCont.appendChild(document.createTextNode(Array.from(set)[i]));selectQuer.append(optionCont)}};let generatSpeiz=arr=>{selectAndButtonMaker(arr);let arrOfYear=["all"];let arrOfSpez=["all"];for(let i=0;i<arr.length;i++){const filters=getEventFilters(arr[i]);filters.forEach(filter=>{arrOfSpez.push(filter)});const year=arr[i].year;arrOfYear.push(year)}
let setOfSpez=new Set(arrOfSpez);let setOfYear=new Set(arrOfYear);spezOptionMaker(setOfSpez);yearOptionMaker(setOfYear);let filterButton=document.querySelector(".selectButton");filterButton.addEventListener("click",()=>{generateFilterd(arr)})};let generateFilterd=arr=>{let typeCond=document.querySelector(".select").value;let yearCond=document.querySelector(".select2").value;let filterdArr=[];for(let i=0;i<arr.length;i++){const eventFilters=getEventFilters(arr[i]);const matchesFilter=typeCond==="all"||eventFilters.includes(typeCond);const matchesYear=yearCond==="all"||arr[i].year===yearCond;if(matchesFilter&&matchesYear){filterdArr.push(arr[i])}}
if(filterdArr.length>0){controlCenter(0,filterdArr)}else{makePopup(typeCond,yearCond);console.log("no events")}};let makePopup=(typeCond,yearCond)=>{let popCont=document.createElement("div");popCont.innerHTML=`Sorry their is'nt an event for (${typeCond}) in (${yearCond}) try use another filter`;document.querySelector(".cards-container").prepend(popCont);popCont.classList.add("popup");select.addEventListener("change",()=>{document.querySelector(".cards-container").removeChild(popCont)});select2.addEventListener("change",()=>{document.querySelector(".cards-container").removeChild(popCont)})};async function oldEventsSection(data){let arrFromRe=data.oldEvents;for(let i=0;i<arrFromRe.length;i++){smallEventArr.push(arrFromRe[i]);if(i===arrFromRe.length-1){controlCenter(0,smallEventArr);generatSpeiz(smallEventArr)}}}
oldEventsSection(data);console.log(data);console.log(data.newEvents);let newEvents=document.querySelector(".event");let image=document.querySelectorAll(".event-image-cont img:nth-child(2)");let buttonCont=document.querySelector(".daysButton");async function newEventsSection(data){let newE=data.newEvents;if(Array.from(newE).length>0){newEvents.classList.remove("dis");console.log(newE);buttonMakerCont(Array.from(newE).length,Array.from(newE))}else{console.log("no events")}}
let buttonMaker=(number,img)=>{let button=document.createElement("button");button.append(document.createTextNode(`day ${number + 1}`));button.addEventListener("click",()=>{if(image[0])image[0].setAttribute("loading","lazy");if(image[1])image[1].setAttribute("loading","lazy");image[0].src=normalizeCloudinaryUrl(img.img1);image[1].src=normalizeCloudinaryUrl(img.img2)});if(number===0){button.click()}
buttonCont.append(button)};let buttonMakerCont=(num,imgLinks)=>{for(let i=0;i<num;i++){buttonMaker(i,imgLinks[i])}};newEventsSection(data);async function getFooterData(){let reponse=data.footer[0];let footerText=document.querySelector("footer .container .col-1 p");footerText.textContent=reponse.text;let footerLocation=document.querySelector("footer .container .col-3 div:first-of-type span");footerLocation.textContent=reponse.location;let footerTimeWork=document.querySelector("footer .container .col-3 div:nth-of-type(2) span");footerTimeWork.textContent=reponse.timeWork;let footerPhone=document.querySelector("footer .container .col-3 div:nth-of-type(3) span");footerPhone.textContent=reponse.phone}
getFooterData()}else{console.log("No such document!")}},error=>{console.error("Error fetching document:",error)})}
loadAndDisplayDataForUser();let BurgerLinks=document.querySelector(".burger-links");BurgerLinks.addEventListener("click",()=>{BurgerLinks.classList.toggle("clicked")});let header=document.querySelector("header");let slideNav=document.querySelector(".slide-nav");BurgerLinks.addEventListener("click",()=>{if(BurgerLinks.classList.contains("clicked")){slideNav.style.left="0";header.classList.add("hoverd")}else{slideNav.style.left="-1000px";header.classList.remove("hoverd")}});document.querySelectorAll(".slide-nav a").forEach(link=>{link.addEventListener("click",()=>{slideNav.style.left="-1000px";header.classList.remove("hoverd");BurgerLinks.classList.remove("clicked")})});document.querySelectorAll("section").forEach(sec=>{sec.addEventListener("click",()=>{slideNav.style.left="-1000px";header.classList.remove("hoverd");BurgerLinks.classList.remove("clicked")})});let imagesArr=document.querySelectorAll(".landing-sec .image-container img");let politsContainer=document.querySelector(".images-polits ul");for(let i=0;i<imagesArr.length;i++){let liPolit=document.createElement("li");politsContainer.appendChild(liPolit)}
let bulletsArr=document.querySelectorAll(".images-polits ul li");let current=0;let first;function set(){first=setInterval(()=>{current++;if(current===imagesArr.length){current=0}
removeAll();imagesArr[current].classList.add("active");bulletsArr[current].classList.add("active")},3000)}
set();function removeAll(){imagesArr.forEach(img=>{img.classList.remove("active")});bulletsArr.forEach(p=>{p.classList.remove("active")})}
let admineButton=document.querySelector(".adminButton");let adminClosingButton=document.querySelector(".login-closer");admineButton.addEventListener("click",()=>{loginSection.classList.remove("hidden");overlayBackground.classList.remove("hidden");document.body.classList.add("no-scroll");BurgerLinks.classList.remove("clicked");slideNav.style.left="-1000px"});adminClosingButton.addEventListener("click",()=>{removeOverLay()});window.addEventListener("DOMContentLoaded",()=>{formContainer.classList.contains("hidden")?"":formContainer.classList.add("hidden");window.scrollTo(0,0)})
